apply plugin: 'java'

repositories {
    jcenter()
}

test {
    testLogging {
        exceptionFormat = 'full'
    }
}

dependencies {
    compile 'org.slf4j:slf4j-api:1.7.5'
    testCompile 'junit:junit:4.11'
}
